

```
array.push_back(x)     // add to end
array.pop_back()       // remove from end
array.size()           // number of elements
array.empty()          // true if no elements
array[i]               // access element at index i
array.back()           // access last element
swap(array[i], array[j]) // swap two elements
```

